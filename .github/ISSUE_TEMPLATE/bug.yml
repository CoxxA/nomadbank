name: Bug 报告
description: 报告可以稳定复现的问题
title: "bug: "
labels: [bug]
body:
  - type: markdown
    attributes:
      value: 安全漏洞请按 SECURITY.md 私下报告，不要创建公开 Issue。
  - type: input
    id: version
    attributes:
      label: NomadBank 版本
      placeholder: 例如 2.0.0 或提交 SHA
    validations:
      required: true
  - type: dropdown
    id: deployment
    attributes:
      label: 部署方式
      options: [Docker Compose, Docker Run, 发布二进制, 源码运行]
    validations:
      required: true
  - type: input
    id: environment
    attributes:
      label: 系统与客户端环境
      description: 请填写操作系统、CPU 架构，以及访问网页时使用的浏览器和版本。
      placeholder: 例如 Ubuntu 24.04 / amd64 / Chrome 126
    validations:
      required: true
  - type: textarea
    id: steps
    attributes:
      label: 复现步骤
      description: 请使用脱敏数据描述最短复现流程。
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: 预期行为
    validations:
      required: true
  - type: textarea
    id: actual
    attributes:
      label: 实际行为
      description: 请说明实际发生了什么，以及是否每次都能复现。
    validations:
      required: true
  - type: textarea
    id: logs
    attributes:
      label: 日志或截图
      description: 不要提交 Cookie、数据库、密码或真实账户信息。
