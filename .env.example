# Docker Compose 映射到宿主机的端口
NOMADBANK_PORT=8080

# 会话有效天数（1～365）
SESSION_DAYS=30

# 容器进程和日志时区；任务排期使用所有者在界面中配置的 IANA 时区
TZ=Asia/Shanghai

# 发布后取消注释并填写实际存在的版本；生产环境不要长期跟随 latest
# NOMADBANK_VERSION=2.0.0
